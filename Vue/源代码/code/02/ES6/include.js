// var sentence = '今天有下雨吗？';
// var word = '雨';
// var res = sentence.includes(word);
// console.log(res);//true

let arr = ['a', 'b', 'c', 'd','拟合']

arr.includes('b')         // true
arr.includes('b', 1)      // true， 该方法第二个参数表示搜索的起始位置，默认为0
arr.includes('b', 2)      // false
